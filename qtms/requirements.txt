# QTMS core dependencies (all free / local; no paid APIs)
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pytest>=7.4
httpx>=0.27          # FastAPI TestClient

# Optional — the system runs WITHOUT these:
# pyarrow            # faster Parquet storage (falls back to CSV)
# qiskit             # real quantum circuit demo (quantum-inspired fallback used otherwise)
# qiskit-aer         # qiskit simulator backend
# ccxt               # future exchange adapter (live trading disabled by default)
# numba              # optional JIT acceleration
